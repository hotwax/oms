<?xml version="1.0" encoding="UTF-8"?>

<entity-facade-xml type="ext">
    <!-- Handy cron strings: [0 0 2 * * ?] every night at 2:00 am, [0 0/15 * * * ?] every 15 minutes, [0 0/2 * * * ?] every 2 minutes -->

    <!-- Service Job to bulk approve Transfer Orders -->
    <moqui.service.job.ServiceJob jobName="bulkApprove_TransferOrders" jobTypeEnumId="BULK_APPROVE_TO"
            description="Service Job to bulk approve the Transfer Orders in OMS"
            serviceName="co.hotwax.oms.order.TransferOrderServices.bulkApprove#TransferOrders"
            cronExpression="0 0 0 * * ?" paused="Y">
        <parameters parameterName="orderIds"/>
    </moqui.service.job.ServiceJob>
</entity-facade-xml>

